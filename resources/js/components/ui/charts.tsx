"use client";
/*
import {
  AreaChart as TremorAreaChart,
  BarChart as TremorBarChart,
  LineChart as TremorLineChart,
  DonutChart as TremorDonutChart,
  ScatterChart as TremorScatterChart,
} from "@tremor/react";

interface ChartProps {
  data: any[];
  index: string;
  categories?: string[];
  category?: string;
  colors?: string[];
  yAxisWidth?: number;
  valueFormatter?: (value: number) => string;
}

export function AreaChart({ data, index, categories, colors, yAxisWidth, valueFormatter }: ChartProps) {
  return (
    <TremorAreaChart
      className="h-full"
      data={data}
      index={index}
      categories={categories}
      colors={colors}
      yAxisWidth={yAxisWidth}
      valueFormatter={valueFormatter}
    />
  );
}

export function BarChart({ data, index, categories, colors, yAxisWidth, valueFormatter }: ChartProps) {
  return (
    <TremorBarChart
      className="h-full"
      data={data}
      index={index}
      categories={categories}
      colors={colors}
      yAxisWidth={yAxisWidth}
      valueFormatter={valueFormatter}
    />
  );
}

export function LineChart({ data, index, categories, colors, yAxisWidth, valueFormatter }: ChartProps) {
  return (
    <TremorLineChart
      className="h-full"
      data={data}
      index={index}
      categories={categories}
      colors={colors}
      yAxisWidth={yAxisWidth}
      valueFormatter={valueFormatter}
    />
  );
}



export function DonutChart({ data, index, category, colors, valueFormatter }: ChartProps) {
  return (
    <TremorDonutChart
      className="h-full"
      data={data}
      index={index}
      category={category}
      colors={colors}
      valueFormatter={valueFormatter}
    />
  );
}

export function ScatterChart({ data, index, category, colors, yAxisWidth, valueFormatter }: ChartProps) {
  return (
    <TremorScatterChart
      className="h-full"
      data={data}
      index={index}
      category={category}
      colors={colors}
      yAxisWidth={yAxisWidth}
      valueFormatter={valueFormatter}
    />
  );
}*/